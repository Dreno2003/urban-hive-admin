export function getFlagEmoji(countryCode: string): string { 
  // Implementation for getting flag emoji based on country code

  return `https://purecatamphetamine.github.io/country-flag-icons/3x2/${countryCode}.svg`
}